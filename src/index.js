/**
 * Mann-Whitney rank test on sets x1 and x2
 *
 * Parameters:
 * x1, x2 - 1D arrays
 */

export function UTest(x1, x2) {
  len_x1 = x1.length;
  len_x2 = x2.length;
  return len_x1, len_x2;
}

var array1 = [1, 3, 5];
var array2 = [2, 4, 6];

l_array1, (l_array2 = UTest(array1, array2));
